import m from "mithril";

const About = {
  view: function () {
    return m("div", "WIP");
    // currently reading:
    // currently listening to:
    // currently obsessed with:
  }
};

export default About;
